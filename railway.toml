[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Wait for migrations + startup (not just process liveness).
healthcheckPath = "/health/ready"
healthcheckTimeout = 180
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
